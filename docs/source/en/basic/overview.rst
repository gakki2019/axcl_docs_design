Overview
========

AXCL documentation is organized around a small set of user journeys:

- understand the runtime surface,
- set up the environment,
- run a minimal initialization flow,
- drill into grouped C/C++ API details.

The content in this repository is placeholder material meant to validate structure, versioning, and API generation.

See also :doc:`install` and :doc:`quick_start`.

Example navigation snippet:

.. code-block:: text

   Basic Docs -> Overview -> Install -> Quick Start
   Dev Docs   -> Architecture -> C/C++ API -> Python Placeholder
