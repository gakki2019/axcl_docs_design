FAQ
===

Why is the API reference in English only?

The generated C/C++ API reference is sourced directly from public header comments, so it is kept in English and not translated in the first phase.

Where are generated files stored?

Generated HTML lives under ``docs/build/html`` and Doxygen XML is written to ``docs/build/doxygen/xml``.
