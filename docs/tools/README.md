# Documentation Tools

The `docs/tools` directory contains maintainer-facing helper scripts.

- `sync_public_headers.sh` copies public headers into `include/external`.
- `gen_api_index.py` is the placeholder entry point for future function-oriented API index generation.
